//Program to find the smallest element of an array
#include<stdio.h>
#include<conio.h>
int main()
{ int CN[10],i,smallest;
prntf("Enter the element of array\n");
for(i=0;i<10;i++)
{ 
scanf("%d",&CN[i]);
}

     printf("The smallest element of array is:");
     smallest=CN[0];
     
     for(i=1;i<10;i=i+1)
     {
     	if(smallest<CN[i])
     	
	 }
}
